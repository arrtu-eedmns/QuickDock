QuickDock.zip
*.zip
Thumbs.db
.DS_Store

# Canvas de design publicado: 2,4 MB de bundle gerado do editor, não é fonte.
# As fontes da marca (.dc.html, canvas.json, .svg) ficam versionadas.
brand/quickdock-logo.html

# Arquivo de trava do Affinity, criado enquanto o .af está aberto
*~lock~

# Chave privada da extensão: define o ID e NUNCA pode ir pro repositório
# (que é público). A metade pública vai no manifest.json, essa não.
*.pem
